{
    "private": true,
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "build": "npm run build -w shared && npm run build -w service-a && npm run build -w service-b && npm run build -w service-c",
      "dev": "pnpm run --parallel dev" 
    },
    "devDependencies": {
      "typescript": "^4.9"
    }
  }
  